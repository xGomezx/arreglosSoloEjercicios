// Use ciclo for, condicional y operación módulo para recorrer el arreglo y mostrar cuales elementos
// son pares

let array1=[3, 50, 70, 600, 40]

for (let index = 0; index < array1.length; index++) {
    if (array1[index]%2==0) {
        console.log(array1[index]);
    }
}